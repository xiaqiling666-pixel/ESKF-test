推荐你按下面顺序继续推进，不要再回到“大而全一起上”的做法。

2026-04-28 收口更新
最低完整离线 ESKF 多源融合闭环已经基本搭建完成。
后续重点不再是继续加模块，
而是：
1. 真实数据调试。
2. 默认配置收敛。
3. 输出 default / debug 边界收口。
4. 论文主线聚焦。

短期不要继续做：
1. 新 batch 细节。
2. manifest / preview / CLI 输出整理。
3. 视觉、激光、因子图新路线。
4. 为了“看起来完整”继续堆状态机规则。

先纠偏一句
当前项目主线不是“把某一组样例数据跑通”，
而是“把项目框架搭对，再让不同数据源挂进来”。
因此下面的路线里：
- 与架构、接口、配置分层有关的内容，属于项目主线
- 与 `00000422`、TCPPP、decoded 目录有关的内容，属于样例验证线

当前已经完成的阶段
第一阶段
原型级 `IMU + GNSS` 基础链路已经搭出第一版。
误差状态传播、GNSS 位置更新和 GNSS 速度更新已经独立出来。

第二阶段
`baro + mag yaw` 已经接入。
不同维度测量更新的写法已经走通。

第三阶段
质量评分、状态机、滞回、防抖、恢复桥接、模式统计和原因统计已经做完第一轮。

第四阶段
协方差健康分类和协方差持续时间已经接入状态机诊断。

你下一阶段最该做的事
第一步
用实际日志或半真实 CSV 跑当前主入口。
先看 input quality、初始化摘要、观测覆盖、时间戳和 lever arm 诊断，
不要先扩新模块。

第二步
收敛默认配置。
这一步要回答：
- 哪些配置属于常规默认主路径
- 哪些配置只是 debug / fallback
- 哪些参数只属于样例配置
- 哪些参数可以进入正式实验配置

第二步半
把论文主线收窄。
这一步要回答：
- 本项目的核心贡献到底是一句话什么
- quality / mode / measurement manager 中谁是主角
- 哪些模块只是支撑验证，不写成贡献点

第三步
做默认输出字段收口。
先定 default / debug 两档，
再决定是否真正删字段。

当前样例验证线说明
截至 2026-04-28，项目已经能直接读：
`DX\05_data\dx_data\decoded\00000422`
这一类 decoded 飞控目录。
这条线目前的意义是：
1. 验证 adapter 设计是否合理
2. 验证 IMU / GNSS 输入语义是否一致
3. 验证统一输入契约是否足够支撑真实样例

它不应该直接替代项目主线。

第四步
在真实日志上做基线对比。
当前已经有第一版 `fusion_policy` 开关，
可以先支持最基础的 ablation：
1. 关闭 `use_nis_rejection / use_adaptive_r / use_recovery_scale`
2. 只开启 `use_nis_rejection`
3. 开启 `use_nis_rejection + use_adaptive_r`
4. 开启完整 measurement manager 策略

当前已经新增对应实验配置模板：
1. `config_experiment_baseline_eskf.json`
2. `config_experiment_nis_reject.json`
3. `config_experiment_adaptive_r.json`
4. `config_experiment_adaptive_r_recovery.json`
5. `config_experiment_full_method.json`

这些模板当前先使用默认 demo 数据，
目的是固化实验开关和结果目录。
后面要做真实数据对比时，
可以复制这些模板并替换 adapter / dataset 配置，
不要直接覆盖通用默认配置。

当前又新增了批处理入口：
`python 02_src\run_experiment_batch.py`

这个入口会顺序运行当前实验配置模板，
并汇总每组 `metrics.csv` 为：
`03_results_experiment_batch\experiment_metrics_summary.csv`

同时会生成紧凑版：
`03_results_experiment_batch\experiment_metrics_key_summary.csv`

建议日常先看紧凑版，
需要追具体原因和状态机细节时，
再看完整 summary。

这一步的意义是：
让 baseline / ablation 从“配置文件存在”
推进到“可以生成统一对比表”。

这里要注意：
状态机目前仍主要是诊断和统计，
还没有真正反作用于滤波更新。
因此不能把“状态机诊断”包装成完整质量驱动融合方法。
下一步如果要往论文主线走，
必须让 quality / mode 进入观测权重或准入策略。

第五步
再考虑加视觉或里程计。
如果你后面要接 `VINS`、`Gazebo` 或 `ROS2` 话题，建议先把视觉结果整理成：
位置
速度
航向
外部位姿
这种统一接口，再接到 `measurements` 里。

更后面的长期路线
1. 如果继续往 KF-GINS 靠拢
   - 继续升级导航主线
   - 从局部 `ENU` 进一步走向更完整的地理坐标建模

2. 如果继续往多传感器工程化靠拢
   - 增加杆臂、外参、时间偏移等状态
   - 接真实数据集和实机日志
   - 建立更稳定的实验配置和阈值管理

你现在最应该做的事
1. 先看 `00_说明\项目主线与样例边界.txt`，把项目主线和样例验证线分开理解。
2. 再看 `00_说明\项目架构说明.txt`，确认模块职责边界。
3. 然后再决定下一步是继续固化框架，还是新增正式数据入口。
