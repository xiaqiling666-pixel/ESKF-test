日期
2026-04-23

项目
ESKF离线融合项目

今日工作记录
1. 今日起点判断
   - 截至 2026-04-22，项目已经完成：
     状态机
     协方差健康诊断
     结果统计与可视化
     自动化回归测试
   - 当前最缺的不是继续扩状态机字段
   - 而是打开真实日志接入入口

2. 今日第一步
   - 将 adapters 从“只支持标准 demo csv”
     重构为“带数据适配器分发入口”
   - 当前已新增：
     dataset_adapter 配置
     load_dataframe_from_config 分发入口
   - 现阶段默认仍使用：
     standard_csv

3. 本步具体修改文件
   - 02_src\eskf_stack\config.py
   - 02_src\eskf_stack\adapters\loader.py
   - 02_src\eskf_stack\adapters\__init__.py
   - 02_src\eskf_stack\app.py
   - 01_data\config.json
   - 05_tests\test_adapters.py

4. 当前这一步已验证
   - unittest: 21 项通过
   - main.py 已重新跑通
   - 现有 demo 数据链路未被破坏

5. 这一步的直接结果
   - 现在主流程已经不再写死只读标准 csv
   - 后面可以继续往 adapters 里挂：
     px4_csv
     ros2_export
     gazebo_export
   - 且不需要先改 core

6. 今日第二步
   - 新增 GREAT-MSF 常用数据适配器：
     great_msf_imu_ins
   - 当前支持：
     GREAT-MSF IMU txt
     GREAT-MSF .ins 结果文件
   - 已完成：
     最近邻时间对齐
     ECEF -> 局部 ENU 位置转换
     ECEF -> 局部 ENU 速度转换
     按首个 .ins 解自动覆盖导航参考点

7. 本步具体修改文件
   - 02_src\eskf_stack\config.py
   - 02_src\eskf_stack\adapters\great_msf_dataset.py
   - 02_src\eskf_stack\adapters\loader.py
   - 02_src\eskf_stack\adapters\__init__.py
   - 02_src\eskf_stack\app.py
   - 05_tests\test_adapters.py

8. 当前这一步已验证
   - unittest: 22 项通过
   - main.py 已重新跑通
   - 已用真实数据完成适配器读取检查：
     DX\04_inputs\great_msf\419\419_IMU_GREAT_agfrd_sow.txt
     DX\06_results\great_msf\419\TCPPP\WNLBS-419-TCPPP-BEST.ins
   - 已用真实数据跑通一次完整管线检查
     输出目录：
     03_results_great_msf_check

9. 本步额外发现
   - GREAT-MSF .ins 作为当前项目里的“直接 GNSS 观测”时
     现有创新门限和测量模型并不匹配
   - 真实检查里虽然整条管线已经能跑
     但当前只接受了很少量的 gnss 更新
   - 说明下一步不该继续改 adapters
   - 而该开始处理：
     GREAT-MSF 结果作为外部轨迹观测时的测量模型和阈值

10. 原始数据检查
   - 已检查飞控原始日志：
     DX\04_inputs\raw\00000422.BIN
   - 已检查定位原始日志：
     UM982_20260405_243\log [2026-04-21_15-37-18].bin

11. 当前识别结果
   - 00000422.BIN 已确认是飞控 DataFlash 二进制日志
   - 其中已经能识别出：
     IMU
     GPS
     BARO
     MAG
     ATT
     XKF
   - 说明它是后面接入飞控原始数据的优先入口

12. UM982 原始文件当前识别结果
   - 该文件不是简单 csv
   - 当前更像串口混合协议流
   - 已识别出：
     #HEADINGA
     $GNGGA
     #GNRMC
     #E6CLOCKFULLBLOCKA
   - 说明它后面可以继续解
   - 但第一优先级不应该高于飞控 DataFlash

13. 这一步的直接结论
   - 后面真正该先做的是：
     从 00000422.BIN 提取 IMU / BARO / MAG / GPS
   - UM982 原始 bin 先作为第二优先级
   - 等飞控原始链路打通后再决定是否直接解 UM982 原始协议

14. 解码后数据链确认
   - 已确认 DX 里存在直接可接的解码后目录：
     05_data\dx_data\decoded\00000422
   - 当前目录包含：
     IMU_Full.csv
     GPS_Raw.csv
     POS_Global_Truth.csv
     XKF_Local_Truth.csv
   - 且这些文件由：
     02_code\extract_code\read_bin.py
     统一生成

15. 今日第三步
   - 新增 decoded 飞控数据适配器：
     dx_decoded_flight_csv
   - 当前支持直接读取：
     IMU_Full.csv
     GPS_Raw.csv
     POS_Global_Truth.csv
     XKF_Local_Truth.csv
   - 已完成：
     GPS 全局坐标 -> 本项目局部 ENU
     Truth 全局坐标 -> 本项目局部 ENU
     XKF 速度 NED -> ENU
     Yaw -> 本项目 yaw 角定义
     以 decoded 目录为输入的统一 DataFrame 构造

16. 本步具体修改文件
   - 02_src\eskf_stack\adapters\dx_decoded_dataset.py
   - 02_src\eskf_stack\adapters\loader.py
   - 05_tests\test_adapters.py

17. 当前这一步已验证
   - unittest: 23 项通过
   - main.py 已重新跑通
   - 已用真实目录完成读取检查：
     DX\05_data\dx_data\decoded\00000422
   - 已用真实目录跑通完整管线检查：
     03_results_dx_decoded_check

18. 本步关键结论
   - 你说得对，当前真正该接的是解码后的数据，不是原始 .BIN
   - 现在项目已经能直接吃这套 decoded 数据目录
   - 但当前 decoded 数据里：
     GPS_Raw.csv 只有位置，没有速度
     也没有现成 baro / mag
   - 所以项目为了跑通，只能临时从 GPS 位置差分构造速度
   - 这会导致真实数据上：
     gnss_vel_rejections 很高
     gnss_pos_rejections 也很高

19. 输入源角色澄清
   - 已确认你当前主要数据形态是：
     提取后的 IMU 数据
     解码后的 obs/nav 数据
   - 这说明项目后面不能只记“adapter kind”
   - 还需要把本次运行到底用了哪些源文件写清楚

20. 今日第四步
   - 新增数据源摘要输出
   - 当前每次运行后都会在 metrics 目录下生成：
     dataset_source_summary.txt
   - 其中会明确记录：
     adapter_kind
     imu_file
     gps_file
     truth_file
     reference_mode
     row_count
     navigation_reference_override

21. 本步具体修改文件
   - 02_src\eskf_stack\adapters\loader.py
   - 02_src\eskf_stack\adapters\dx_decoded_dataset.py
   - 02_src\eskf_stack\adapters\great_msf_dataset.py
   - 02_src\eskf_stack\app.py
   - 05_tests\test_adapters.py

22. 当前这一步已验证
   - unittest: 23 项通过
   - main.py 已重新跑通
   - 已用真实 decoded 目录核对：
     03_results_dx_decoded_check\metrics\dataset_source_summary.txt
   - 当前摘要文件已经能明确写出：
     IMU_Full.csv
     GPS_Raw.csv
     POS_Global_Truth.csv
     XKF_Local_Truth.csv

23. 今日第五步
   - 修正 decoded 飞控数据入口里的观测语义
   - 新增：
     gps_velocity_mode
   - 当前支持：
     derived_from_pos
     none
     from_xkf
   - 同时主流程初始化已放宽为：
     只要有 gnss_pos 就允许初始化
   - 如果没有 gnss_vel：
     先用零速度初始化
     yaw 先用 mag
     没有 mag 时退化为 0

24. 本步具体修改文件
   - 02_src\eskf_stack\adapters\dx_decoded_dataset.py
   - 02_src\eskf_stack\app.py
   - 05_tests\test_adapters.py
   - 05_tests\test_app.py

25. 当前这一步已验证
   - unittest: 25 项通过
   - main.py 已重新跑通
   - 已用真实 decoded 目录做两组对比运行：
     03_results_dx_decoded_compare_derived
     03_results_dx_decoded_compare_pos_only
   - 当前已确认：
     关闭差分伪速度后
     gnss_vel_rejections 从 2845 降到 0
   - 但 gnss_pos_updates 仍只有 4
     gnss_pos_rejections 仍有 2842
   - 说明当前主要矛盾已经从“速度语义错误”
     收敛为“位置观测门限 / 对齐 / 参考一致性”问题

26. 这一步的直接结论
   - 现在项目已经能正确支持：
     只有位置的 decoded GPS 输入
   - 这比之前把位置差分硬当速度更接近真实数据语义
   - 下一步不该继续补速度假量
   - 而应该开始检查：
     GNSS 位置测量噪声
     NIS 门限
     参考点一致性
     时间对齐误差

27. 今日第六步
   - 对 decoded GPS 位置观测问题做定位检查
   - 已额外核对：
     GPS -> IMU 最近邻时间对齐误差
     GPS 与 POS_Global_Truth 的位置一致性
     真实数据上的 GNSS 位置创新增长过程

28. 本步关键检查结果
   - 时间对齐不是主要矛盾：
     平均约 8.8 ms
     最大约 18.1 ms
   - GPS 与 POS_Global_Truth 的平均三维偏差约 1.42 m
   - 说明当前 `gnss_pos` 大面积拒收
     不是因为原始 GPS 完全不可用
   - 更主要是：
     只有位置、没有速度和航向时
     过早初始化会让状态很快漂开

29. 今日第七步
   - 改进主流程初始化策略
   - 当前不再对“只有 gnss_pos、没有 gnss_vel / mag_yaw”的帧直接初始化
   - 改为：
     先缓存首个 GNSS 位置
     等到后续位置满足最小时间差和最小平面位移后
     用两帧位置估计初始速度和初始 yaw
     再进行 bootstrap 初始化

30. 本步具体修改文件
   - 02_src\eskf_stack\app.py
   - 05_tests\test_app.py

31. 当前这一步已验证
   - unittest: 26 项通过
   - main.py 已重新跑通
   - 已用真实 decoded 目录重跑：
     03_results_dx_decoded_compare_pos_only_bootstrap

32. 本步真实数据结果
   - 与旧的 `position-only` 方案相比：
     gnss_pos_rejections 从 2842 降到 2645
     mean_gnss_pos_nis 从 8332.19 降到 8154.02
     final_pos_sigma_norm_m 从 1775174.98 降到 1469240.95
     final_vel_sigma_norm_mps 从 9381.17 降到 6965.74
   - 但：
     gnss_pos_updates 仍然只有 3
   - 说明 bootstrap 初始化方向是对的
     但单靠它还不够

33. 今日第八步
   - 对 decoded GPS 的 `gnss_pos_std / NIS` 门限做了小范围参数扫描
   - 已测试多组：
     0.8 / 1.5 / 2.5 / 4.0 / 6.0 m
     以及更宽的 adapt / reject 门限

34. 本步直接结论
   - 单独放宽位置噪声和门限
     只能把 `gnss_pos_updates` 从 3 提高到 11 左右
   - 但大部分位置观测仍然被拒收
   - 说明“继续单纯调门限”不是主解
   - 后面更值得做的是：
     引入更可靠的初始航向 / 速度来源
     或者把 obs/nav 解进一步整理成更稳定的外部观测输入

35. 今日第九步
   - 新增外部解算结果适配器：
     dx_imu_external_solution
   - 当前支持直接读取：
     IMU_Full.csv + 外部 .ins 解算结果
   - 已支持：
     自动估计 IMU 时间与周内秒之间的时间偏移
     ECEF -> ENU 位置速度转换
     可选写入外部解 yaw
     可选接 decoded 目录里的 truth 字段做对照

36. 本步具体修改文件
   - 02_src\eskf_stack\adapters\dx_external_solution_dataset.py
   - 02_src\eskf_stack\adapters\loader.py
   - 05_tests\test_adapters.py

37. 当前这一步已验证
   - unittest: 27 项通过
   - main.py 已重新跑通
   - 已用真实数据跑通：
     03_results_dx_external_tcppp_422
     03_results_dx_external_lcppp_422

38. 本步关键发现
   - 新外部解算入口虽然跑通
     但拒收仍然很高
   - 进一步看前几次更新可见：
     两次 GNSS 更新之间
     状态在不到 1 秒内就明显向下发散
   - 这说明问题不只是门限
     更像 IMU 输入语义本身不匹配

39. 今日第十步
   - 核对 demo 生成逻辑和 mechanization 后确认：
     当前滤波器吃的是“比力”
     静止时 z 轴应接近 +9.81
   - 而 `IMU_Full.csv` 静止段是接近 -9.82
   - 因此新增 IMU 轴系变换入口：
     imu_transform_mode
   - 当前支持：
     raw
     ardupilot_frd_to_flu
     flip_z_accel
     negate_all_accel

40. 本步具体修改文件
   - 02_src\eskf_stack\adapters\imu_transform.py
   - 02_src\eskf_stack\adapters\dx_decoded_dataset.py
   - 02_src\eskf_stack\adapters\dx_external_solution_dataset.py
   - 05_tests\test_adapters.py

41. 当前这一步已验证
   - unittest: 28 项通过
   - main.py 已重新跑通
   - 已用真实数据完成对比：
     03_results_dx_decoded_pos_only_frd_to_flu
     03_results_dx_external_tcppp_422_frd_to_flu

42. 本步真实数据结论
   - 对当前 `00000422` 这组数据
     推荐的 IMU 变换模式是：
     ardupilot_frd_to_flu
   - 对 decoded position-only 路线
     改完后结果出现质变：
     gnss_pos_updates: 3 -> 2648
     gnss_pos_rejections: 2645 -> 0
     mean_gnss_pos_nis: 8154.02 -> 0.0496
     final_pos_sigma_norm_m: 1469240.95 -> 0.6516
     final_vel_sigma_norm_mps: 6965.74 -> 0.5329
   - 对外部 TCPPP 路线
     也明显改善，但仍不如 decoded position-only 稳定：
     gnss_pos_updates: 1 -> 18
     gnss_pos_rejections: 1037 -> 1020
     gnss_vel_rejections: 1037 -> 1027
   - 所以当前阶段的主结论是：
     先把 IMU 轴系语义修正对
     你现在手头的 decoded GPS position-only 路线就已经可用

43. 今日第十一步
   - 在保留通用配置不动的前提下
     新增 `00000422` 专用运行配置
   - 当前已新增：
     01_data\config_00000422_decoded.json
     04_tools\运行00000422_推荐配置.bat
   - 同时主入口已支持：
     `python 02_src\main.py <配置文件路径>`

44. 本步直接结果
   - 以后默认 demo / 通用配置
     仍走：
     01_data\config.json
   - `00000422` 推荐真实数据配置
     单独走：
     01_data\config_00000422_decoded.json
   - 后面如果继续做 `00000419` / `00000418`
     可以按同样方式继续扩专用配置

45. 今日第十二步
   - 把统一输入契约正式落成代码约束
   - 当前新增：
     02_src\eskf_stack\adapters\contract.py
   - 统一输入契约已明确区分：
     必需字段
     可选标准字段
     可保留扩展字段

46. 本步具体修改文件
   - 02_src\eskf_stack\adapters\contract.py
   - 02_src\eskf_stack\adapters\csv_dataset.py
   - 02_src\eskf_stack\adapters\loader.py
   - 05_tests\test_adapters.py
   - 00_说明\统一输入契约.txt
   - 00_说明\README_先看我.txt
   - 00_说明\项目架构说明.txt

47. 本步直接结果
   - 所有 adapter 返回的数据
     现在都会在进入主流程前统一过契约检查
   - 缺少必需字段会直接报错
   - 缺少可选字段会自动补 NaN
   - 输出会统一按 time 排序
   - 这说明项目主线第一次有了“可执行的统一输入契约”
     不再只是文档约定

48. 今日第十三步
   - 把配置分层正式落成代码约束
   - 当前新增：
     config_metadata
   - 当前允许的配置角色：
     default_general
     sample_validation
     experiment

49. 本步具体修改文件
   - 02_src\eskf_stack\config.py
   - 02_src\eskf_stack\app.py
   - 01_data\config.json
   - 01_data\config_00000422_decoded.json
   - 01_data\配置说明.txt
   - 00_说明\README_先看我.txt
   - 05_tests\test_config.py

50. 本步直接结果
   - `01_data\config.json`
     现在被代码层明确约束为：
     default_general
   - 非默认单独配置
     不能再冒充主配置
   - 运行结果里的：
     dataset_source_summary.txt
     现在也会写出：
     config_path
     config_profile
     config_name
     config_purpose
   - 说明后面看结果目录时
     已经能直接分辨：
     这是通用配置结果
     还是样例验证结果

51. 当前这一步已验证
   - unittest: 32 项通过
   - main.py 已重新跑通
   - 已核对：
     03_results\metrics\dataset_source_summary.txt
     现在已经能写出配置角色信息

52. 今日第十四步
   - 把统一输入契约进一步细化成三层字段分级
   - 当前已明确分成：
     核心输入层
     可选观测层
     诊断/真值层

53. 本步具体修改文件
   - 02_src\eskf_stack\adapters\contract.py
   - 02_src\eskf_stack\adapters\__init__.py
   - 05_tests\test_adapters.py
   - 00_说明\统一输入契约.txt
   - 00_说明\项目架构说明.txt

54. 本步直接结果
   - `time / ax ay az / gx gy gz`
     现在被明确标为：
     核心输入层
   - `gnss_* / baro_h / mag_yaw`
     现在被明确标为：
     可选观测层
   - `truth_*`
     现在被明确标为：
     诊断/真值层
   - 这说明后面再接新数据源时
     不会再把评估字段和主观测字段混为一谈

55. 这一步为什么重要
   - 如果不做这一步
     后面很容易把：
     `truth_x`
     这种字段误塞回主流程
   - 现在三层边界已经更清楚：
     哪些字段主流程必须要
     哪些字段有就更新
     哪些字段只是评估用

56. 当前这一步已验证
   - unittest: 33 项通过
   - main.py 已重新跑通

57. 今日第十五步
   - 把模块消费边界进一步落成代码结构
   - 当前已新增：
     观测视图
     真值视图
     analysis 真值访问器

58. 本步具体修改文件
   - 02_src\eskf_stack\adapters\csv_dataset.py
   - 02_src\eskf_stack\adapters\__init__.py
   - 02_src\eskf_stack\measurements\base.py
   - 02_src\eskf_stack\app.py
   - 02_src\eskf_stack\analysis\truth_access.py
   - 02_src\eskf_stack\analysis\evaluator.py
   - 02_src\eskf_stack\analysis\plotter.py
   - 05_tests\test_app.py
   - 00_说明\项目架构说明.txt
   - 00_说明\统一输入契约.txt
   - 00_说明\README_先看我.txt

59. 本步直接结果
   - `measurements`
     现在从类型边界上只接观测视图
   - 观测视图里只有：
     gnss_pos
     gnss_vel
     baro_h
     mag_yaw
   - `analysis`
     现在通过统一真值访问器读取 `truth_*`
   - 说明项目主线已经从：
     字段分层
     进一步推进到：
     模块消费分层

60. 这一步为什么重要
   - 如果只在文档里说：
     measurements 不要碰 truth
     这条边界很容易被后续代码破坏
   - 现在至少在代码结构上
     这条边界已经更明确
   - 后面继续扩测量模型时
     不容易再把评估字段混进主滤波逻辑

61. 当前这一步已验证
   - unittest: 34 项通过
   - main.py 已重新跑通

62. 今日第十六步
   - 把观测更新里的鲁棒逻辑从具体量测模块里抽出来
   - 当前已新增：
     measurement manager
   - 当前 manager 统一负责：
     NIS
     门限拒绝
     自适应 R
     连续拒绝后的恢复阶段渐进重接入

63. 本步具体修改文件
   - 02_src\eskf_stack\measurements\base.py
   - 02_src\eskf_stack\measurements\manager.py
   - 02_src\eskf_stack\measurements\gnss_position.py
   - 02_src\eskf_stack\measurements\gnss_velocity.py
   - 02_src\eskf_stack\measurements\barometer.py
   - 02_src\eskf_stack\measurements\mag_yaw.py
   - 02_src\eskf_stack\measurements\__init__.py
   - 02_src\eskf_stack\app.py
   - 05_tests\test_measurements.py
   - 00_说明\README_先看我.txt
   - 00_说明\项目架构说明.txt
   - 00_说明\后续扩展路线.txt

64. 本步直接结果
   - 各量测模块现在主要负责：
     residual
     H
     base_R
   - 鲁棒决策不再散在各个量测文件里
   - 主流程结果里新增了：
     management_mode
     recovery_scale
   - 后面如果做论文里的：
     gating
     adaptive R
     recover
     mode switch
     更适合继续长在 manager 这一层

65. 这一步为什么重要
   - 这是把“量测模型”
     和“鲁棒观测管理”
     正式拆开
   - 这样后面扩新传感器时
     不用每个文件都自己重写一套拒绝和恢复逻辑
   - 也更符合项目主线：
     先把框架边界搭稳
     再逐步补初始化和工程增强项

66. 当前这一步已验证
   - 新增 measurement manager 单元测试：
     拒绝
     自适应 R
     渐进恢复
   - 完整回归已通过：
     unittest
     main.py

67. 今日第十七步
   - 新增静止粗对准初始化模块
   - 当前已新增：
     静止窗口配置
     roll/pitch 粗估
     gyro bias 初估
     accel bias 粗估
   - 并把这层接回主流程初始化

68. 本步具体修改文件
   - 02_src\eskf_stack\config.py
   - 01_data\config.json
   - 01_data\config_00000422_decoded.json
   - 02_src\eskf_stack\core\math_utils.py
   - 02_src\eskf_stack\core\initialization.py
   - 02_src\eskf_stack\core\filter.py
   - 02_src\eskf_stack\core\__init__.py
   - 02_src\eskf_stack\app.py
   - 05_tests\test_initialization.py
   - 05_tests\test_app.py
   - 05_tests\test_config.py
   - 00_说明\README_先看我.txt
   - 00_说明\项目架构说明.txt
   - 01_data\配置说明.txt

69. 本步直接结果
   - 初始化不再默认只有：
     位置
     速度
     yaw
     bias 全零
   - 当前如果起始段满足静止窗口条件：
     会先用 IMU 静止段估计 roll/pitch
     会估计 gyro bias
     会给 accel bias 一个粗初值
   - 主流程运行后，
     `dataset_source_summary.txt`
     里也会写出这次是否用了静止粗对准初始化

70. 这一步为什么重要
   - 这一步补的是你当前最明显的短板之一：
     初始化太弱
   - 现在系统不必再默认：
     roll = 0
     pitch = 0
     bias = 0
   - 但也明确保留了工程边界：
     纯静止段对横向 accel bias 不完全可观
     所以这里只能叫粗估
     不能假装已经做成完整标定

71. 当前这一步已验证
   - 新增初始化单元测试：
     静止粗对准估计 roll/pitch
     gyro bias 初估
     filter.initialize 接收 roll/pitch/bias
   - 全量 unittest：39 项通过
   - main.py 已重新跑通

72. 今日第十八步
   - 补姿态方向一致性单元测试
   - 当前已新增：
     纯旋转传播测试
     小角度误差注入方向测试

73. 本步具体修改文件
   - 05_tests\test_navigation_core.py
   - 00_说明\README_先看我.txt
   - 00_说明\项目架构说明.txt

74. 本步直接结果
   - 当前 `predict()` 在：
     纯 z 轴角速度
     静止比力补偿
     无地球自转
     的场景下
     能按预期累积 yaw
     且不会凭空产生平移
   - 当前 `_inject_error_state()`
     已明确验证为：
     右乘误差注入
     `q <- q ⊗ delta_q`
   - 并且它和左乘结果可区分

75. 这一步为什么重要
   - 姿态方向约定最怕“代码能跑但符号不一致”
   - 现在至少把最关键的两件事钉死了：
     传播方向
     注入方向
   - 后面如果继续改 mechanization
     或改 reset Jacobian
     这些测试会第一时间报警

76. 当前这一步已验证
   - 姿态相关测试通过
   - 完整回归已通过：
     unittest
     main.py

77. 今日第十九步
   - 给传播层加入 `dt` 防护
   - 当前已新增：
     `dt` 配置项
     最小正步长检查
     最大步长检查
     大步长跳过或截断策略
     传播诊断输出

78. 本步具体修改文件
   - 02_src\eskf_stack\config.py
   - 01_data\config.json
   - 01_data\config_00000422_decoded.json
   - 02_src\eskf_stack\core\filter.py
   - 02_src\eskf_stack\app.py
   - 02_src\eskf_stack\analysis\evaluator.py
   - 05_tests\test_config.py
   - 05_tests\test_navigation_core.py
   - 05_tests\test_metrics.py
   - 01_data\配置说明.txt
   - 00_说明\README_先看我.txt
   - 00_说明\项目架构说明.txt

79. 本步直接结果
   - 传播不再默认“所有 dt 都照单全收”
   - 当前会明确区分：
     nonpositive_dt
     below_min_positive_dt
     above_max_dt_skipped
     above_max_dt_clamped
     applied
   - 主流程结果当前可记录：
     dt_raw_s
     dt_applied_s
     predict_skipped
     predict_warning
     predict_reason

80. 这一步为什么重要
   - 真实日志最常见的问题之一
     不是公式推导
     而是时间戳异常
   - 这一步先把传播层做成“有边界、有诊断”的状态
   - 还没上更重的 van Loan
     但已经先把最容易炸系统的时间步长问题拦住

81. 当前这一步已验证
   - 针对性测试通过：
     配置加载
     纯旋转传播
     dt 跳过
     dt 截断
     dt 指标统计
   - 全量 unittest：44 项通过
   - 这次未再主动跑主入口生成新结果文件

82. 今日额外项目检查
   - 已检查仓库跟踪文件和工作区污染来源
   - 当前确认两个工程卫生问题：
     仓库仍跟踪 `__pycache__ / .pyc`
     仓库仍跟踪 `03_results`
   - 这会导致：
     运行测试后工作区容易变脏
     运行主入口后结果文件容易被误认为源码改动

83. 今日第二十步
   - 开始做仓库卫生整治
   - 当前已新增：
     `.gitignore`
   - 当前已明确忽略：
     `__pycache__`
     `*.pyc`
     `.idea`
     `03_results`
     `03_results_*`

84. 本步具体修改文件
   - .gitignore
   - 04_tools\运行核心检查.bat
   - 04_tools\一键运行示例.bat
   - 00_说明\README_先看我.txt

85. 本步直接结果
   - “跑测试”和“跑完整结果”现在在工具提示上被明确区分
   - 日常开发路径：
     `运行核心检查.bat`
   - 需要看结果时再跑：
     `一键运行示例.bat`
   - 以后新生成的缓存和结果目录
     不该再继续进入 Git 跟踪

86. 今日第二十一步
   - 做初始化链路和观测管理层的收口检查
   - 当前确认的主线问题是：
     measurement manager 虽然已经存在
     但此前统一 policy 只真正覆盖了 GNSS
     `baro / mag` 还没有接上配置化门控阈值

87. 本步具体修改文件
   - 02_src\eskf_stack\config.py
   - 01_data\config.json
   - 01_data\config_00000422_decoded.json
   - 02_src\eskf_stack\measurements\barometer.py
   - 02_src\eskf_stack\measurements\mag_yaw.py
   - 05_tests\test_config.py
   - 05_tests\test_measurements.py
   - 01_data\配置说明.txt
   - 00_说明\项目架构说明.txt
   - 00_说明\README_先看我.txt

88. 本步直接结果
   - 当前 `innovation_management`
     不再只定义 GNSS 阈值
   - 已补充：
     `baro_nis_adapt_threshold`
     `baro_nis_reject_threshold`
     `mag_yaw_nis_adapt_threshold`
     `mag_yaw_nis_reject_threshold`
   - `baro` 和 `mag_yaw`
     现在也真正接入了 manager 的统一：
     NIS
     reject
     adaptive R

89. 这一步为什么重要
   - 这次查出来的问题不是代码跑不通
     而是框架边界还没完全收口
   - 如果只让 GNSS 走统一 policy
     `baro / mag` 还是各走各的
     那 measurement manager 就还不算真正完成
   - 现在这层才更接近“所有现有观测统一受管”

90. 当前这一步已验证
   - 针对性测试通过：
     config 阈值存在
     baro 触发 reject
     mag 触发 adaptive R
   - 全量 unittest：46 项通过

91. 今日第二十二步
   - 把初始化从“黑箱成功/失败”推进成可诊断状态机
   - 当前已新增：
     初始化阶段
     初始化原因
     heading 来源
     static alignment 状态

92. 本步具体修改文件
   - 02_src\eskf_stack\core\initialization.py
   - 02_src\eskf_stack\core\__init__.py
   - 02_src\eskf_stack\app.py
   - 05_tests\test_app.py
   - 01_data\配置说明.txt
   - 00_说明\项目架构说明.txt
   - 00_说明\README_先看我.txt

93. 本步直接结果
   - 当前初始化链路不再只是：
     能不能初始化
   - 现在会明确区分：
     `WAITING_GNSS`
     `WAITING_BOOTSTRAP_MOTION`
     `READY_DIRECT_INIT`
     `READY_BOOTSTRAP_INIT`
     `INITIALIZED`
   - 并且会记录：
     `initialization_reason`
     `heading_source`
     `static_alignment_ready`
     `static_alignment_reason`

94. 这一步为什么重要
   - 这一步吸收了外部建议里有价值的一点：
     不要让初始化继续保持黑箱
   - 但实现方式还是按我们自己的主线做：
     不是重写滤波
     而是先把初始化流程变成可解释状态
   - 后面不管接真实日志还是做样例对比
     都更容易知道系统卡在什么环节

95. 当前这一步已验证
   - 初始化状态相关测试通过：
     等 GNSS
     ready direct init
     等 bootstrap motion
   - 全量 unittest：49 项通过

96. 今日第二十三步
   - 给初始化状态机补等待超时和回退策略
   - 当前已新增：
     heading 等待超时
     bootstrap 阈值配置化
     zero yaw 回退开关

97. 本步具体修改文件
   - 02_src\eskf_stack\config.py
   - 01_data\config.json
   - 01_data\config_00000422_decoded.json
   - 02_src\eskf_stack\core\initialization.py
   - 02_src\eskf_stack\app.py
   - 05_tests\test_app.py
   - 05_tests\test_config.py
   - 01_data\配置说明.txt
   - 00_说明\项目架构说明.txt
   - 00_说明\README_先看我.txt

98. 本步直接结果
   - 当前初始化不再默认无限等待 heading 来源
   - 已支持：
     `heading_wait_timeout_s`
     `zero_yaw_fallback_enabled`
   - 如果配置允许，
     且等待超过阈值，
     可以退化为：
     `zero_yaw_fallback`
     的 direct init
   - 当前默认仍然关闭这条回退，
     所以项目默认行为不会被激进改掉

99. 这一步为什么重要
   - 这一步把初始化从“有阶段”
     推进到“有控制策略”
   - 现在系统不仅会说：
     我为什么还没初始化
   - 还会说：
     我最多等多久
     超时后是否允许退化回退

100. 当前这一步已验证
   - 初始化相关测试新增通过：
     timeout zero yaw fallback ready
     timeout 后允许 direct init
   - 全量 unittest：51 项通过

101. 今日第二十四步
   - 把初始化状态摘要正式接进 metrics / analysis
   - 当前已新增运行级初始化指标：
     `initialization_mode_direct_flag`
     `initialization_mode_bootstrap_position_pair_flag`
     `initialization_static_coarse_alignment_used_flag`
     `initialization_static_alignment_ready_flag`
     `initialization_zero_yaw_fallback_used_flag`
     `initialization_wait_s`

102. 本步具体修改文件
   - 02_src\eskf_stack\analysis\evaluator.py
   - 02_src\eskf_stack\app.py
   - 05_tests\test_metrics.py

103. 本步直接结果
   - 初始化不再只是：
     `dataset_source_summary.txt`
     里的文本摘要
   - 现在也能进入：
     `metrics.csv`
     `metrics_summary.txt`
   - 这说明后面做实验对比时，
     可以直接把初始化方式和等待时长纳入回归基线。

104. 这一步为什么重要
   - 初始化是单次运行级事件，
     不适合伪装成逐时刻 `result_df` 序列。
   - 所以这次采用的是：
     保持 `result_df` 继续承载时序诊断
     用 `initialization_summary` 承载运行级初始化语义
     再由 `analysis` 统一吸收成 metrics
   - 这样结构更清楚，
     也更适合后面继续扩初始化质量指标。

105. 当前这一步已验证
   - `python -m unittest 05_tests.test_metrics` 通过
   - `python -m unittest discover 05_tests` 通过
   - 全量 unittest：52 项通过

106. 今日第二十五步
   - 补 `app` 级初始化闭环回归测试
   - 当前新增覆盖：
     direct 初始化
     bootstrap_position_pair 初始化
     zero yaw fallback
     static coarse alignment used
     初始化摘要和 metrics 一致性

107. 本步具体修改文件
   - 05_tests\test_app.py

108. 本步直接结果
   - 初始化这条线现在不只测：
     “状态是否 ready”
   - 还测：
     “最终初始化摘要写了什么”
     “metrics 标量是否一致”
   - 这让初始化真正进入了结构化回归，而不是停在局部函数测试。

109. 本步额外发现
   - 如果测试里不补足默认要求的静止样本数，
     `static coarse alignment`
     会稳定停在：
     `not_enough_static_samples`
   - 因此针对 bootstrap + static alignment 的回归，
     需要显式构造满足默认窗口条件的静止 IMU 样本。

110. 当前这一步已验证
   - `python -m unittest 05_tests.test_app` 通过
   - `python -m unittest discover 05_tests` 通过
   - 全量 unittest：54 项通过

111. 今日第二十六步
   - 把初始化分析输出从“只有裸 metric 键值”
     推进为“人类可读初始化摘要 + metric 键值”
   - 当前 `metrics_summary.txt`
     顶部新增：
     `Initialization Summary`
   - 当前摘要包括：
     phase
     mode
     reason
     heading_source
     static_coarse_alignment_used
     static_alignment_ready
     static_alignment_reason
     zero_yaw_fallback_used
     initialization_wait_s

112. 本步具体修改文件
   - 02_src\eskf_stack\analysis\evaluator.py
   - 02_src\eskf_stack\app.py
   - 05_tests\test_metrics.py
   - 00_说明\README_先看我.txt
   - 00_说明\项目架构说明.txt
   - 01_data\配置说明.txt

113. 本步直接结果
   - 当前看结果时，
     不需要先去翻：
     `dataset_source_summary.txt`
     再自己对应 metric 键名
   - 现在在：
     `metrics_summary.txt`
     顶部就能直接看到这次初始化到底走的是：
     direct
     还是 bootstrap_position_pair
   - 也能直接看到：
     是否用了 static coarse alignment
     是否触发了 zero yaw fallback
     初始化前等待了多久

114. 这一步为什么重要
   - 这一步补的是“分析可读性”，
     不是继续扩某个样例数据的专用参数。
   - 初始化这类单次事件，
     如果只保留裸指标，
     后面看实验结果很容易忘记这次到底怎么起的滤波器。
   - 现在 analysis 输出里已经把：
     结构化回归
     人类可读摘要
     文档说明
     这三层统一起来了。

115. 当前这一步已验证
   - 新增 `save_metrics` 初始化摘要测试通过
   - `python -m unittest 05_tests.test_metrics` 通过
   - `python -m unittest discover 05_tests` 通过
   - 全量 unittest：55 项通过
